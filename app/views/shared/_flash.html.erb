<% if flash.any? %>
  <div class="flashes">
    <% flash.each do |type, message| %>
      <div class="flash flash--<%= type == "alert" ? "alert" : "notice" %>"><%= message %></div>
    <% end %>
  </div>
<% end %>
