<% content_for :title, "Nouveau compte — Altea" %>

<header class="page-head">
  <div>
    <span class="page-head__eyebrow"><%= area_meta(life_area)[:label] %> · Banque</span>
    <h1>Nouveau compte</h1>
  </div>
</header>

<section class="panel">
  <%= render "form", account: @account %>
</section>
